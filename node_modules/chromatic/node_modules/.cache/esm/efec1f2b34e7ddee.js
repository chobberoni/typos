let baseStorybookUrl;_34e‍.x([["initial",()=>initial],["pending",()=>pending],["success",()=>success],["failed",()=>failed]]);_34e‍.w("../../lib/utils",[["baseStorybookUrl",["baseStorybookUrl"],function(v){baseStorybookUrl=v}]]);

       const initial = {
  status: 'initial',
  title: 'Create tunnel',
};

       const pending = ctx => ({
  status: 'pending',
  title: 'Opening tunnel to Chromatic capture servers',
  output: `Connecting to ${ctx.env.CHROMATIC_TUNNEL_URL}`,
});

       const success = ctx => ({
  status: 'success',
  title: `Opened tunnel to Chromatic capture servers`,
  output: `Connected to ${baseStorybookUrl(ctx.cachedUrl || ctx.isolatorUrl)}`,
});

       const failed = ctx => ({
  status: 'error',
  title: 'Opening tunnel to Chromatic capture servers',
  output: `Could not reach ${baseStorybookUrl(ctx.cachedUrl || ctx.isolatorUrl)}`,
});
