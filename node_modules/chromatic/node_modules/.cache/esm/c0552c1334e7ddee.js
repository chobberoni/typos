let createTask,transitionTo,listingStories,storybookPublished,buildLimited,paymentRequired,snapshotQuotaReached,initial,pending,runOnly,success;_34e‍.x([["setEnvironment",()=>setEnvironment],["createBuild",()=>createBuild],["default",()=>_34e‍.o]]);_34e‍.w("../lib/tasks",[["createTask",["createTask"],function(v){createTask=v}],["transitionTo",["transitionTo"],function(v){transitionTo=v}]]);_34e‍.w("../ui/messages/info/listingStories",[["default",["listingStories"],function(v){listingStories=v}]]);_34e‍.w("../ui/messages/info/storybookPublished",[["default",["storybookPublished"],function(v){storybookPublished=v}]]);_34e‍.w("../ui/messages/warnings/buildLimited",[["default",["buildLimited"],function(v){buildLimited=v}]]);_34e‍.w("../ui/messages/warnings/paymentRequired",[["default",["paymentRequired"],function(v){paymentRequired=v}]]);_34e‍.w("../ui/messages/warnings/snapshotQuotaReached",[["default",["snapshotQuotaReached"],function(v){snapshotQuotaReached=v}]]);_34e‍.w("../ui/tasks/verify",[["initial",["initial"],function(v){initial=v}],["pending",["pending"],function(v){pending=v}],["runOnly",["runOnly"],function(v){runOnly=v}],["success",["success"],function(v){success=v}]]);







const TesterCreateBuildMutation = `
  mutation TesterCreateBuildMutation($input: CreateBuildInput!, $isolatorUrl: String!) {
    createBuild(input: $input, isolatorUrl: $isolatorUrl) {
      id
      number
      specCount
      snapshotCount
      componentCount
      webUrl
      cachedUrl
      reportToken
      features {
        uiTests
        uiReview
      }
      wasLimited
      app {
        account {
          exceededThreshold
          paymentRequired
          billingUrl
        }
        repository {
          provider
        }
        setupUrl
      }
      snapshots {
        spec {
          name
          component {
            name
            displayName
          }
        }
        parameters {
          viewport
          viewportIsDefault
        }
      }
    }
  }
`;

       const setEnvironment = async ctx => {
  // We send up all environment variables provided by these complicated systems.
  // We don't want to send up *all* environment vars as they could include sensitive information
  // about the user's build environment
  ctx.environment = JSON.stringify(
    Object.entries(process.env).reduce((acc, [key, value]) => {
      if (ctx.env.ENVIRONMENT_WHITELIST.find(regex => key.match(regex))) {
        acc[key] = value;
      }
      return acc;
    }, {})
  );

  ctx.log.debug(`Got environment ${ctx.environment}`);
};

       const createBuild = async (ctx, task) => {
  const { client, environment, git, log, pkg, cachedUrl, isolatorUrl, options } = ctx;
  const { list, only, patchBaseRef, patchHeadRef, preserveMissingSpecs } = options;
  const { version, matchesBranch, ...commitInfo } = git; // omit some fields
  const autoAcceptChanges = matchesBranch(options.autoAcceptChanges);

  if (only) {
    transitionTo(runOnly)(ctx, task);
  }

  const { createBuild: build } = await client.runQuery(TesterCreateBuildMutation, {
    input: {
      ...commitInfo,
      ...(only && { only }),
      autoAcceptChanges,
      cachedUrl,
      environment,
      patchBaseRef,
      patchHeadRef,
      preserveMissingSpecs,
      packageVersion: pkg.version,
      storybookVersion: ctx.storybook.version,
      viewLayer: ctx.storybook.viewLayer,
      addons: ctx.storybook.addons,
    },
    isolatorUrl,
  });

  ctx.build = build;
  ctx.isPublishOnly = !build.features.uiReview && !build.features.uiTests;
  ctx.isOnboarding = build.number === 1 || (build.autoAcceptChanges && !autoAcceptChanges);

  if (list) {
    log.info(listingStories(build.snapshots));
  }

  if (build.wasLimited) {
    const { account } = build.app;
    if (account.exceededThreshold) {
      log.warn(snapshotQuotaReached(account));
      ctx.exitCode = 101;
    } else if (account.paymentRequired) {
      log.warn(paymentRequired(account));
      ctx.exitCode = 102;
    } else {
      // Future proofing for reasons we aren't aware of
      log.warn(buildLimited(account));
      ctx.exitCode = 100;
    }
  }

  transitionTo(success, true)(ctx, task);

  if (list || ctx.isPublishOnly || matchesBranch(options.exitOnceUploaded)) {
    ctx.exitCode = 0;
    ctx.skipSnapshots = true;
    ctx.log.info(storybookPublished(ctx));
  }
};

_34e‍.d(createTask({
  title: initial.title,
  skip: ctx => ctx.skip,
  steps: [transitionTo(pending), setEnvironment, createBuild],
}));
