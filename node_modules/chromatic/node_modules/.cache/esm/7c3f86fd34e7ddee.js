_34e‍.x([["initial",()=>initial],["pending",()=>pending],["lookupMergeBase",()=>lookupMergeBase],["checkoutMergeBase",()=>checkoutMergeBase],["installingDependencies",()=>installingDependencies],["success",()=>success]]);const initial = {
  status: 'initial',
  title: 'Prepare workspace',
};

       const pending = ctx => ({
  status: 'pending',
  title: 'Preparing your workspace',
  output: `Ensuring your git workspace is clean and up-to-date`,
});

       const lookupMergeBase = ctx => ({
  status: 'pending',
  title: 'Preparing your workspace',
  output: `Looking up the git merge base for '${ctx.options.patchHeadRef}' on '${ctx.options.patchBaseRef}'`,
});

       const checkoutMergeBase = ctx => ({
  status: 'pending',
  title: 'Preparing your workspace',
  output: `Checking out merge base commit '${ctx.mergeBase.substr(0, 7)}'`,
});

       const installingDependencies = ctx => ({
  status: 'pending',
  title: 'Preparing your workspace',
  output: 'Installing dependencies',
});

       const success = ctx => ({
  status: 'success',
  title: `Prepared your workspace`,
  output: `Checked out commit '${ctx.mergeBase.substr(0, 7)}' on '${ctx.options.patchBaseRef}'`,
});
