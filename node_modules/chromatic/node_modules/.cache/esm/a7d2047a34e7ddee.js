let writeFile,confirm,addedScript,notAddedScript,scriptNotFound;_34e‍.x([["default",()=>checkPackageJson]]);_34e‍.w("jsonfile",[["writeFile",["writeFile"],function(v){writeFile=v}]]);_34e‍.w("node-ask",[["confirm",["confirm"],function(v){confirm=v}]]);_34e‍.w("../ui/messages/info/addedScript",[["default",["addedScript"],function(v){addedScript=v}]]);_34e‍.w("../ui/messages/info/notAddedScript",[["default",["notAddedScript"],function(v){notAddedScript=v}]]);_34e‍.w("../ui/messages/warnings/scriptNotFound",[["default",["scriptNotFound"],function(v){scriptNotFound=v}]]);






const scriptName = 'chromatic';

const findScript = scripts =>
  scripts[scriptName] || Object.values(scripts).find(cmd => cmd.startsWith(scriptName));

               async function checkPackageJson({ log, options, packageJson, packagePath }) {
  if (!options.interactive) return;

  try {
    const json = { ...packageJson };
    if (!json.scripts) json.scripts = {};
    if (findScript(json.scripts)) return;

    const scriptCommand = `${`npx chromatic ${options.originalArgv.slice(2).join(' ')}`
      .replace(/--project-token[= ]\S+/, '')
      .replace(/--app-code[= ]\S+/, '')
      .trim()} --project-token ${options.projectToken}`;

    log.info('');
    if (await confirm(scriptNotFound(scriptName))) {
      json.scripts[scriptName] = scriptCommand;
      await writeFile(packagePath, json, { spaces: 2 });
      log.info('');
      log.info(addedScript(scriptName));
    } else {
      log.info('');
      log.info(notAddedScript(scriptName, scriptCommand));
    }
  } catch (e) {
    log.warn(e);
  }
}
